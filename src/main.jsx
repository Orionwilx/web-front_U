import React from 'react';
import ReactDOM from 'react-dom/client';
import Bar from './component/nav/Navbar';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
	<React.StrictMode>
		<Bar />
	</React.StrictMode>,
);
